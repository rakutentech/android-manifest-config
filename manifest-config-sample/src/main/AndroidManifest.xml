<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.rakuten.tech.mobile.manifestconfig.sample">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true">
    <meta-data
      android:name="RawInt"
      android:value="100"/>
    <meta-data
      android:name="BoxedInt"
      android:value="200"/>
    <meta-data
      android:name="RawBoolean"
      android:value="true"/>
    <meta-data
      android:name="BoxedBoolean"
      android:value="true"/>
    <meta-data
      android:name="String"
      android:value="Some string 😬"/>
    <meta-data
      android:name="RawFloat"
      android:value="1.1"/>
    <meta-data
      android:name="BoxedFloat"
      android:value="1.2"/>

    <activity
      android:label="@string/title_activity_manifest_config"
      android:name=".ManifestConfigActivity"
      android:theme="@style/AppTheme.NoActionBar">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>
</manifest>